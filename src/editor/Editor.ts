const ProjectLoaded = new Event('ProjectLoaded'); // Called when the engine has loaded the project name into file
const PluginHostReady = new Event('PluginHostReady'); // Called when the pluginHost is loaded
const PluginsFinishedLoad = new Event('PluginsFinishedLoad'); // Called when all plugins have finished authenticating
